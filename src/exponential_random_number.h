#ifndef EXPONENTIAL_RANDOM_NUMBER_H
#define EXPONENTIAL_RANDOM_NUMBER_H

class ExponentialRandomNumber
{
public:
    // Returns an exponential random number based on the given mean (it doesn't do that currently)
    static double GetRandomNumber(double mean);
};

#endif